# Cloud sync is disabled until a deployed API endpoint is configured.
# Local development with `npm run cloud:dev` uses the Vite proxy on port 3210 below.
VITE_CLOUD_API_BASE=/api/cloud
VITE_PLATFORM_API_BASE=/api/platform
VITE_SOCIAL_API_BASE=/api/social
